# Render deployment configuration
services:
  - type: web
    name: clipit-backend
    runtime: docker
    plan: free
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
